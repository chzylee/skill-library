# dev-build configuration for this repo — read by the dev-build skill and
# scripts/dev-build-check.sh. Plain sh KEY="value"; see maintenance/dev-build/.
skills_root="."
stable_branch="main"
dev_branch="dev"
release_steps="add ./<skill> to the skills array in .claude-plugin/plugin.json; add a README catalog-table row (show for edit first); build dist/<skill>.skill (skip for hook-installer skills)"
